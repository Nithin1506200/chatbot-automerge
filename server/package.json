{
  "name": "automerge-chatbot",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "tsnd --respawn --transpile-only -r tsconfig-paths/register src/index.ts",
    "test": "tsnd --respawn --transpile-only -r tsconfig-paths/register src/automerge.test.ts"
  },
  "devDependencies": {
    "@types/node": "^18.16.3",
    "@types/ws": "^8.5.4",
    "dotenv": "^16.0.3",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@automerge/automerge": "^2.0.3",
    "ws": "^8.13.0"
  }
}
